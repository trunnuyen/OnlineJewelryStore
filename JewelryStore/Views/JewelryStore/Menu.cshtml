@model IEnumerable<JewelryStore.Models.Menu>
@using JewelryStore.Models;
@{

    Customer acc = (Customer)Session["usr"];
    string username = "";
    if (Request.Cookies["usr"] != null)
    {
        username = Request.Cookies["usr"].Value;

    }

}

<div class="collapse navbar-collapse" id="navbarSupportedContent">

    <ul class="navbar-nav">
        @foreach (var item in Model)
        {
            <li class="dropdown submenu">
                <a class="dropdown-toggle" href="@Url.Action(@item.Link,"JewelryStore")" role="button" aria-haspopup="true" aria-expanded="false">@item.Name</a>
                <ul class="dropdown-menu">
                    @Html.Action("ItemMenuType", "JewelryStore", new { id = item.ID })
                    @Html.Action("Brandtype", "JewelryStore", new { id = item.ID })

                </ul>
            </li>






        }
        <li class="search_icon"><a class="popup-with-zoom-anim" href="#test-search"><i class="icon icon-Search"></i></a></li>
        @Html.Action("CartPartial", "Cart")
        @*@if (Session["usr"] == null)
            {
                <li><a href="@Url.Action("Login","Login")">Login/</a></li>


                <li><a href="@Url.Action("Register","Login")">Register</a></li>

            }
            else
            {

                <li class="dropdown submenu">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
                        <span>@acc.Name</span>
                        <img width="50" height="50" style="border-radius: 50%" src="@Url.Content("~/img/Item/avatar-default-icon.png")">
                    </a>

                    <ul class="dropdown-menu">
                        <li><a href="/Client/ProfileClient">Profile</a></li>
                        <li><a href="~/Account/EditPassword">Change Pasword</a></li>
                        <li><a href="@Url.Action("SignOut","Jewelry")">Logout</a></li>
                    </ul>
                </li>


            }*@

    </ul>

</div>
